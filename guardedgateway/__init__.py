"""GuardedGateway — an OpenAI-compatible LLM gateway that cannot overspend and
cannot leak PHI: pre-call dollar caps against a durable ledger, PHI redaction,
and BAA-only routing for PHI-flagged traffic."""

__version__ = "0.1.0"
